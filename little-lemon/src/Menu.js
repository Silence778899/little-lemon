import React from 'react';

function Menu() {
    return (
        <div>
            <h1>This is the Menu with specials and links to order / reserve tables</h1>
        </div>
    );

};

export default Menu;